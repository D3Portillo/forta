{
  "name": "forta",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "clean:all": "./scripts/clean-all.sh",
    "build": "turbo run build",
    "network": "turbo run --filter=network dev"
  },
  "devDependencies": {
    "turbo": "latest"
  },
  "packageManager": "npm@7.24.2"
}
