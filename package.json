{
  "name": "forta",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "turbo run test",
    "clean:all": "./scripts/clean-all.sh",
    "build:network": "turbo run --filter=network build",
    "start:network": "turbo run --filter=network start",
    "test:network": "turbo run --filter=network test"
  },
  "devDependencies": {
    "turbo": "latest"
  },
  "packageManager": "npm@7.24.2"
}
